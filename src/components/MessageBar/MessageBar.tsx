import React, { FC } from 'react';

import styles from './MessageBar.module.css';

/**
 * @TODO This component is basically blank right now
 * It's future role will be to display top-level user messages.
 * Think: 'This site saves cookies' type of message
 */
const MessageBar = () => (
  <section className={styles.main} />
);

export default MessageBar as FC;
